🎬 NLP Movie Recommendation System

A content-based movie recommendation system built using Natural Language Processing (NLP) and Machine Learning.
The system recommends movies similar to a given movie based on cast and director information.

🚀 Features

Content-based movie recommendations

NLP-based feature engineering

Cosine similarity for matching movies

Simple and fast recommendation function

Built using Python and scikit-learn

🧠 How It Works

Movie metadata is loaded from the TMDB dataset.

Important features (cast and director) are extracted.

Text features are combined into tags.

CountVectorizer converts text into numerical vectors.

Cosine similarity computes similarity between movies.

The system recommends top similar movies.
